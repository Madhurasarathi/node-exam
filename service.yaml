apiVersion: v1
kind: Service
metadata:
  labels:
    app: nodeexam
  name: nodeexam
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: nodeexam
